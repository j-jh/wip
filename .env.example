# Copy to .env (gitignored) and edit locally.
#   cp .env.example .env

# Path to dd-cli if it is not on PATH (optional).
# DD_CLI_BIN=/path/to/dd-cli

# Must be true to allow order submit (charges money).
# Even with -confirm-submit, SubmitOrder refuses unless this is true.
WIP_ALLOW_SUBMIT=false
